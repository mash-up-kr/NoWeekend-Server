oauth:
  google:
    client-id: GoogleClientId
    client-secret: GoogleSecret
    token-base-url: https://oauth2.googleapis.com
    resource-base-url: https://www.googleapis.com
  apple:
    app-id: AppleAppId
    key-id: AppleKeyId
    team-id: AppleTeamId
    api-url: https://appleid.apple.com
    secret-key-file-path: AppleSecretKeyPath

spring.cloud.openfeign:
  client:
    config:
      example-api:
        connectTimeout: 2100
        readTimeout: 5000
        loggerLevel: full
  compression:
    response:
      enabled: false
  httpclient:
    max-connections: 2000
    max-connections-per-route: 500

---
spring.config.activate.on-profile: local

---
spring.config.activate.on-profile:
  - local-dev
  - dev

---
spring.config.activate.on-profile:
  - staging
  - live

example:
  api:
    url: https://live.example.example
